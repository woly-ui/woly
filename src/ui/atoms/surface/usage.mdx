---
name: surface
category: atoms
package: 'woly'
---

import { SurfaceExample } from './example';

`Surface` is a simple layer that contains information. Surface can be used inside Popover or Sidebar.

### Example

<SurfaceExample />

```tsx
const Example = () => <Surface>Surface content</Surface>;
```

### Kinds

| Name      | Description  |
| --------- | ------------ |
| `Surface` | Base surface |

### Props

| Name       | Type              | Default     | Description                                                       |
| ---------- | ----------------- | ----------- | ----------------------------------------------------------------- |
| `variant`  | `string`          | `'default'` | Variant prop to style Popover component                           |
| `children` | `React.ReactNode` |             | Pass any content as a children property within Surface component. |
