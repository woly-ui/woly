---
name: surface
category: atoms
package: 'woly'
---

import * as surface from './index';
import { HighlightCode } from '../../../lib/highlight';
import { SurfaceExample } from './example';

`surface` is a simple layer that contains information. Surface can be used inside Popover or Sidebar.

### Example

<SurfaceExample />

export const Example = `
<surface.Base>Surface content</surface.Base>
`.trim();

<HighlightCode code={Example} />

### Kinds

| Name   | Description  |
| ------ | ------------ |
| `Base` | Base surface |

### Props

Pass any content as a children property within Surface component.
