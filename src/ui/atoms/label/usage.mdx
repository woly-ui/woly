---
name: label
category: atoms
package: 'woly'
---

import { Global } from 'box-styles';
import { Label } from 'ui';

`Label` is a description text for located near interactive component.

### Example

<Global>
  <Label variant="base">Base label</Label>
</Global>

```tsx
const Example = () => <Label text="Base label" variant="base" />;
```

### Kinds

| Name    | Description                                         |
| ------- | --------------------------------------------------- |
| `Label` | Base label. Useful for creating a new kind of label |

### Props

| Name          | Type               | Default     | Description                                                                   |
| ------------- | ------------------ | ----------- | ----------------------------------------------------------------------------- |
| `children`    | `React.ReactNode`  |             | Label content                                                                   |
| `variant`     | `string`           | `'default'` | Variant prop to style Input component                                         |
| `...`         | `HTMLLabelElement` | `{}`        | Other props are inherited from `HTMLLabelElement`                             |
