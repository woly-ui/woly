---
name: headerpanel
category: atoms
package: 'woly'
---

import { Content, Playground, block, State, FormBlock, Aside, Menu } from 'box-styles'
import { Surface, Button, Input, Label, ButtonIcon, HeaderPanel} from 'ui'
import { CloseIcon } from 'icons';

`HeaderPanel` is a navigation component that displays information and actions relating to the current screen.

<Playground>
     <HeaderPanel>Hello! It`s a HeaderPanel</HeaderPanel>
</Playground>
 
### Example



<Playground>
  <State initial={false} change={(i) => !i}>
    {(value, change) => (
      <block.S>
        <HeaderPanel>
          <div style={{paddingRight: "10px"}}>
            <Button onClick={change} text="Sign in" variant="primary" />
          </div>
          <div>
            <b><a href="" target="" style={{paddingRight: "10px"}}>This is a link</a></b>
            <b><a href="" target="" style={{paddingRight: "10px"}}>This is a link</a></b>
            <b><a href="" target="" >This is a link</a></b>
          </div>
        </HeaderPanel>
        <div 
          style={{
            position: 'absolute',
                visibility: value ? 'visible' : 'hidden',
                width: value ? '250px' : '0',
                marginTop: '20px',
             
          }}>
            <Surface>
              <FormBlock>
                <Label variant="base">Name</Label>
                  <Input
                    name="name"
                    onChange={(event) => console.info('On input change')}
                    placeholder="Ivan Ivanov"
                    type="text"
                    variant="base"
                  />
                <Label variant="base">Password</Label>
                  <Input
                    name="password"
                    onChange={(event) => console.info('On input change')}
                    type="password"
                    variant="base"
                  />
                  <Button
                    style={{ marginTop: '20px' }}
                    onClick={() => {}}
                    text="Sign in"
                    variant="primary"
                  />
              </FormBlock>
            </Surface>
          </div>
        </block.S>
      )}
   </State>
</Playground>

### Kinds

| Name          | Description      |
| ------------- | ---------------- |
| `HeaderPanel` | Base HeaderPanel |

### Props

| Name       | Type              | Default     | Description                                                           |
| ---------- | ----------------- | ----------- | --------------------------------------------------------------------- |
| `variant`  | `string`          | `'default'` | Variant prop to style HeaderPanel component                           |
| `children` | `React.ReactNode` |             | Pass any content as a children property within HeaderPanel component. |
