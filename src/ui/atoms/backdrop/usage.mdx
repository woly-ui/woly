---
name: backdrop
category: atoms
package: 'woly'
---

import {Playground, State} from 'lib/playground'
import {Button, Field, Input, Modal} from 'ui'

`Backdrop` is a dark layer that covers full screen. It can be used in Modal windows or Aside menu panel.

### Example

Modal based on `Backdrop`.

<Playground>
  <State initial={false} change={(i) => !i}>
    {(value, change) => (
      <>
        <Button onClick={change} text="Open modal" />
        <Modal visible={value} title="Change customer" onClose={change} variant="primary">
          <form autoComplete="off">
            <Field label="Customer name">
              <Input
                variant="primary"
                name="name"
                onChange={(event) => console.info('On name change')}
                placeholder="Name"
                type="text"
              />
            </Field>
            <Field label="Customer email">
              <Input
                variant="primary"
                name="email"
                onChange={(event) => console.info('On email change')}
                placeholder="Email"
                type="text"
              />
            </Field>
            <div style={{ display: 'flex', justifyContent: 'space-between' }}>
              <Button onClick={() => alert('Change clicked!')} text="Change" variant="primary" />
              <Button onClick={change} text="Cancel" variant="primary" outlined />
            </div>
          </form>
        </Modal>
      </>
    )}
  </State>
</Playground>

### Props

| Name      | Type     | Default       | Description                              |
| --------- | -------- | ------------- | ---------------------------------------- |
| `variant` | `string` | `'secondary'` | Variant prop to style Backdrop component |
