---
name: checkbox
category: atoms
package: 'woly'
---

import { CheckboxExample } from './example';
import { HighlightCode } from '../../../lib/highlight';

`checkbox` is used to turn on/off an option. Checkbox can be provided with label using `label` component.

### Example

<CheckboxExample />

export const Example = `
  <checkbox.Base
    label={<label.Primary text="Checkbox label" />}
    isChecked={true}
    onChange={(e) => console.info('change', e)}
    id="checkbox"
  />
`.trim();

<HighlightCode code={Example} />

### Kinds

| Name   | Description                                                 |
| ------ | ----------------------------------------------------------- |
| `Base` | Base checkbox. Useful for creating a new kind of checkboxes |

### Props

| Name        | Type                                         | Default | Description                        |
| ----------- | -------------------------------------------- | ------- | ---------------------------------- |
| `id`        | `string`                                     |         | HTML id attribute                  |
| `isChecked` | `boolean`                                    |         | Whether checkbox is checked or not |
| `label`     | `React.ReactNode`                            | `null`  | Label component                    |
| `onChange`  | `React.ChangeEventHandler<HTMLInputElement>` |         | Callback when checkbox is clicked  |
