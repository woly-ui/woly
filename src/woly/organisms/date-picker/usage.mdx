import {Playground, StateEvent} from 'lib/playground';
import {DatePicker} from 'ui';
import {IconInfo} from 'static/icons';
import dayjs from 'dayjs';

# DatePicker

DatePicker is used to select or input the date.
You can type the date in the input field or select it from the calendar that appears near the input.

### Mouse actions

By clicking on the input, the field calendar window shows up. By clicking on the date in the calendar, you set the chosen date in the input field.

### Keyboard actions

Pressing Enter on the selected input field will trigger the appearance of the calendar. When the calendar is opened, pressing Tab will focus inside calendar. You can navigate between elements of the calendar by pressing arrows on the keyboard. Pressing Enter key will set the chosen date inside input field.

<Playground>
  <StateEvent initial="12.05.1993" change={(value) => value}>
    {(value, change) => (
      <div style={{ padding: '250px 0', display: 'flex' }}>
        <div style={{ padding: '0 5px' }}>
          <DatePicker
            icon={<IconInfo />}
            initialDate={value}
            isOpen
            name="open-picker"
            placeholder="13.10.2013"
            priority="primary"
            onChange={change}
            disabledDates={(date) => date.day() === 0}
            locale="ru"
          />
        </div>
        <div>
          <DatePicker
            placeholder="13.10.2013"
            priority="primary"
            icon={<IconInfo />}
            format="MM.YYYY"
            disabled
          />
        </div>
      </div>
    )}
  </StateEvent>
</Playground>

### Choose a date from the range between 1 and 15 day of the month

<Playground>
  <StateEvent initial="12.05.1993" change={(value) => value}>
    {(value, change) => (
      <div style={{ padding: '250px 0', display: 'flex' }}>
        <div style={{ padding: '0 5px' }}>
          <DatePicker
            icon={<IconInfo />}
            initialDate={value}
            isOpen
            name="open-picker"
            placeholder="13.10.2013"
            priority="primary"
            onChange={change}
            disabledDates={(date) => date.date() > 15}
          />
        </div>
      </div>
    )}
  </StateEvent>
</Playground>

| Name            | Type              | Default        | Description                                     |
| --------------- | ----------------- | -------------- | ----------------------------------------------- |
| `disabled`      | `boolean`         | `false`        | Disable DatePicker                              |
| `disabledDates` | `fn`              |                | Callbacl to set disabled dates                  |
| `format`        | `string`          | `'DD.MM.YYYY'` | Format of the date                              |
| `icon`          | `React.ReactNode` |                |                                                 |
| `initialDate`   | `dayjs.Dayjs`     |                | Initial selected date                           |
| `isOpen`        | `boolean`         | `'false'`      | Whether the calendar dropdown is visible or not |
| `locale`        | `string`          | `'ru'`         | Locale of calendar                              |
| `name`          | `string`          |                | Name attribute                                  |
| `placeholder`   | `string`          |                | Placeholder attribute                           |
| `priority`      | `string`          | `'secondary'`  | Priority prop to style Popover component        |
